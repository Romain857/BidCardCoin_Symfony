<nav class="navbar navbar-expand-lg navbar-light bg-secondary">
    <a class="navbar-brand" href="{{ path("home") }}"><i class="fas fa-gavel"></i> BidCardCoin</a>
    <a class="navbar-brand" href="{{ path("enchere_index") }}"><i class="fas fa-signal-stream"></i> Encheres en live</a>
    <a class="navbar-brand" href="{{ path("lot_index") }}">Lots</a>
    <a class="navbar-brand" href="{{ path("produit_index") }}">Produits</a>

    {% if app.user %}
        <a class="navbar-brand" href="{{ path("produit_new") }}"><i class="fas fa-bullseye"></i> Vendre</a>
        <div class="navbar-nav ml-auto">
            <a class="navbar-brand">{{ app.user.username }}</a>
            <a href="{{ path('logout') }}" class="navbar-brand nav-link"><i class="fas fa-sign-out-alt"></i> Deconnexion</a>
        </div>
    {% else %}
        <div class="navbar-nav ml-auto">
            <a class="navbar-brand" href="{{ path("inscription") }}"><i class="fas fa-user"></i></a>
            <a href="{{ path('login') }}" class="navbar-brand nav-link"><i class="fas fa-sign-in-alt"></i> Connexion</a>
        </div>
    {% endif %}

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
</nav>
